<% if (mensagens && mensagens.length > 0) { %>
  <div class="mensagens" role="status" aria-live="polite">
    <% mensagens.forEach(function (mensagem) { %>
      <p class="alerta alerta--<%= mensagem.tipo %>"><%= mensagem.texto %></p>
    <% }); %>
  </div>
<% } %>
