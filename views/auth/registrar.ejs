<section class="abertura">
  <h1 class="abertura__titulo">Criar conta</h1>
  <p class="abertura__linha">
    Escolha como você vai usar o EventHub: organizando eventos ou se inscrevendo neles.
  </p>
</section>

<form class="formulario" action="/auth/registrar" method="POST" novalidate>
  <div class="campo-grupo">
    <label for="nome">Nome</label>
    <input
      class="campo <%= erros.nome ? 'campo--invalido' : '' %>"
      type="text"
      id="nome"
      name="nome"
      value="<%= valores.nome %>"
      maxlength="120"
      autocomplete="name"
      required
    />
    <% if (erros.nome) { %>
      <span class="campo-erro"><%= erros.nome %></span>
    <% } else { %>
      <span class="campo-ajuda">É o nome que aparece para quem organiza o evento.</span>
    <% } %>
  </div>

  <div class="campo-grupo">
    <label for="email">E-mail</label>
    <input
      class="campo <%= erros.email ? 'campo--invalido' : '' %>"
      type="email"
      id="email"
      name="email"
      value="<%= valores.email %>"
      maxlength="180"
      autocomplete="email"
      required
    />
    <% if (erros.email) { %>
      <span class="campo-erro"><%= erros.email %></span>
    <% } %>
  </div>

  <div class="campo-grupo">
    <label for="senha">Senha</label>
    <input
      class="campo <%= erros.senha ? 'campo--invalido' : '' %>"
      type="password"
      id="senha"
      name="senha"
      autocomplete="new-password"
      required
    />
    <% if (erros.senha) { %>
      <span class="campo-erro"><%= erros.senha %></span>
    <% } else { %>
      <span class="campo-ajuda">Pelo menos 8 caracteres.</span>
    <% } %>
  </div>

  <div class="campo-grupo">
    <label for="confirmacaoSenha">Confirmar senha</label>
    <input
      class="campo <%= erros.confirmacaoSenha ? 'campo--invalido' : '' %>"
      type="password"
      id="confirmacaoSenha"
      name="confirmacaoSenha"
      autocomplete="new-password"
      required
    />
    <% if (erros.confirmacaoSenha) { %>
      <span class="campo-erro"><%= erros.confirmacaoSenha %></span>
    <% } %>
  </div>

  <div class="campo-grupo">
    <label for="papel">Como você vai usar</label>
    <select
      class="campo <%= erros.papel ? 'campo--invalido' : '' %>"
      id="papel"
      name="papel"
      required
    >
      <option value="" <%= valores.papel === '' ? 'selected' : '' %>>Selecione</option>
      <option value="participante" <%= valores.papel === 'participante' ? 'selected' : '' %>>
        Quero me inscrever em eventos
      </option>
      <option value="organizador" <%= valores.papel === 'organizador' ? 'selected' : '' %>>
        Quero publicar eventos
      </option>
    </select>
    <% if (erros.papel) { %>
      <span class="campo-erro"><%= erros.papel %></span>
    <% } else { %>
      <span class="campo-ajuda">Isso define o que você vê depois de entrar.</span>
    <% } %>
  </div>

  <div class="acoes">
    <button class="botao botao--primario" type="submit">Criar conta</button>
    <a class="botao botao--texto" href="/auth/login">Já tenho conta</a>
  </div>
</form>
